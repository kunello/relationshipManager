# ── Required ──
# The only email allowed to access the MCP server
ALLOWED_EMAIL=you@gmail.com

# ── GCP ──
# Create a GCP project: https://console.cloud.google.com
GOOGLE_PROJECT_ID=your-gcp-project-id

# ── Google OAuth ──
# Create an OAuth 2.0 Client ID in Google Cloud Console:
#   APIs & Services → Credentials → Create Credentials → OAuth client ID
#   Application type: Web application
#   Authorized redirect URIs: add your Cloud Run URL + /callback
GOOGLE_CLIENT_ID=123456789.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-...

# ── Storage ──
# Name of the GCS bucket that stores contacts.json and interactions.json
DATA_BUCKET_NAME=your-project-crm-data

# ══════════════════════════════════════════════
# Azure Configuration (for azure/ deployment)
# ══════════════════════════════════════════════

# ── Azure Subscription ──
AZURE_SUBSCRIPTION_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
AZURE_RESOURCE_GROUP=personal-crm-rg
AZURE_LOCATION=eastus

# ── Azure Storage ──
# Storage account name (lowercase, alphanumeric, 3-24 chars, globally unique)
AZURE_STORAGE_ACCOUNT_NAME=yournamecrmdata
# Blob container name within the storage account
AZURE_CONTAINER_NAME=crm-data

# ── Microsoft Entra ID (Azure AD) ──
# Create an App Registration in Azure Portal:
#   Entra ID → App registrations → New registration
#   Supported account types: Single tenant
#   Redirect URI: https://claude.ai/api/mcp/auth_callback (Web platform)
AZURE_CLIENT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
AZURE_CLIENT_SECRET=your-client-secret-value
AZURE_TENANT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
